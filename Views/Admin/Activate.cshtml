@{
    ViewBag.Title = "Activate User";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}
@model EADWebTeacherFacilitationSystem.Models.User
<body>
    <div class="container">
        @using (Html.BeginForm("ActivateUser", "Admin", FormMethod.Post))
        {

            @Html.LabelFor(u     => u.Id, new { @required = "" })
            @Html.TextBoxFor(u => u.Id)

            <input type="submit" value="Activate" />
        }
    </div>
</body>
